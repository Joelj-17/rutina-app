# Netlify solo publica la carpeta "publico".
# Todo lo que esté fuera de ella (la carpeta "personal") NUNCA se sube a internet.

[build]
  publish = "publico"
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
